[build-system]
requires = ["setuptools>=42", "wheel", "setuptools_scm[toml]>=6.0,<8"]
build-backend = "setuptools.build_meta"


[tool.mypy]
mypy_path = "${MYPY_CONFIG_FILE_DIR}/stubs"

[tool.ruff]
line-length = 100
exclude = ["WDL/_version.py"]

[tool.ruff.lint]
ignore = ["E741", "E731"]
